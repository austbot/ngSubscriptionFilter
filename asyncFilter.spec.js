describe('async filter', () => {

  it('Only Accepta observables as input', () => {

  });
  
  it('Subscribes to the event stream', () => {
    
  });
  
  it('Initates a digest when the observable emits', () => {
    
  });
  
  it('Throws when the subscription has an error', () => {
    
  });
  
  it('Shuts down the subscription on the complete event', () =>{
    
  });
  
  it('Removes the stateful flag when the observable is complete', () => {
    
  });
});